# --- Python ---
__pycache__/
*.py[cod]
.venv/
/.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
/dist/
/build/

# --- Node / frontend ---
/frontend/node_modules/
/frontend/dist/
/frontend/.vite/

# --- marginalia runtime (never commit user data or secrets) ---
/data/
providers.toml

# --- tooling ---
/.code-review-graph/
# code-review-graph + per-editor MCP config (machine-specific; recreate with `code-review-graph install`)
.mcp.json
.opencode.json
.claude/settings.json
.claude/skills/*.md
# rtk per-project config (recreate with `rtk init`)
.rtk/
.claude/settings.local.json
.claude/worktrees/
.env
.env.local
*.log

# --- OS / editor ---
.DS_Store
Thumbs.db
